# Django Plugin for ProjectCLI

## Installation
```shell
project plugins:install projectcli/django
```

## Usage
Create a new Django application in a fresh Django project:
```shell
project help django:create
```

Run artisan commands:
```shell
project help django:admin
```
